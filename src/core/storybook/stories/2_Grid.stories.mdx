import { Meta, Preview } from '@storybook/addon-docs/blocks'
import styles from '../../../styles/global/grid.css';

<Meta title="Getting Started|Styles/Grid System" />

## Examples

<Preview style={{
  padding: `var(--space-xs) var(--space-sm)`
}}>
  <div className="c-grid-container">
    <div class="c-grid-row">
      <div class="c-grid-col">
        1 of 2
      </div>
      <div class="c-grid-col">
        2 of 2
      </div>
    </div>
  </div>
</Preview>


# Screens

DS Grid System and their CSS Variables.

| Size      | Variable            | Device              |
| --------- | ------------------- | ------------------- |
| <  576px  | `var(--screen-xs)`  | Mobile              |
| >= 768px  | `var(--screen-sm)`  | Tablet              |
| >= 992px  | `var(--screen-md)`  | Desktop             |
| >= 1280px | `var(--screen-lg)`  | Large Desktop       |
| >= 1440px | `var(--screen-xlg)` | Extra Large Desktop |

We also has a variable to define the max size of containers.

| Size   | Variable                      |
| ------ | ----------------------------- |
| 1140px | `var(--screen-max-container)` |

And a variable to define the column's gutter width.

| Size | Variable                     |
| ---- | ---------------------------- |
| 16px | `var(--screen-gutter-width)` |